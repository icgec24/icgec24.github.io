import 'package:flutter/material.dart';

class KeynoteSpeechTemplate extends StatelessWidget {
  const KeynoteSpeechTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
